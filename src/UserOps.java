//this will contain all possible user operations.
//Different operator types will have to implement this.
//Each User class will contain an object called operator, which will contain
//implementations of the functions that the user can perforrm.
interface UserOps
{
    SaveFile();

}
